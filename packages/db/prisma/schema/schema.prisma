generator client {
  provider      = "prisma-client"
  output        = "../generated"
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
  moduleFormat  = "esm"
  runtime       = "nodejs"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}
